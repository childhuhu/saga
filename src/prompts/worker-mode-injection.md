You are now in **worker mode** for this saga stage.

Execute the stage task using the appropriate tools for your domain. When done, call `saga_advance` with `workerFinished: true` and attach artifacts.

{{TOOL_HINTS}}
